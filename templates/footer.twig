<footer class="footer">
  <div class="container">
    <img src="{{options.logo_white}}" class="footer__logo" alt="{{site.name}}">
  </div>
</footer>
<div class="modal" id="modal-form">
  <div class="modal__box modal__box_default">
    <div class="modal__header">
      <p class="modal__title">Заказать услугу</p>
      <button class="modal__close" data-close="modal">&times;</button>
    </div>
    <div class="modal__body">
      {{function('do_shortcode', '[contact-form-7 id="332" title="Общая форма"]')}}
    </div>
  </div>
</div>
<div class="modal" id="modal-form-appear">
  <div class="modal__box modal__box_default">
    <div class="modal__header">
      <button class="modal__close" data-close="modal">&times;</button>
      <p class="modal__title">“Получите бесплатную запись вебинара “Вещи-ловушки женского гардероба”<br><span>Для этого заполните форму ниже</span></p>
    </div>
    <div class="modal__body">
      {{function('do_shortcode', '[contact-form-7 id="450" title="Имя(фамилия), емайл, телефон - модальное окно"]')}}
    </div>
    <div class="modal__footer">
      <button class="modal__footer-close">больше не показывать это окно</button>
    </div>
  </div>
</div>